# Prebuilt app artifacts

После запуска workflow `Build macOS app bundle` через **Run workflow**,
в репозитории обновляется файл:

- `prebuilt/app-macos.zip`

Это архив с готовым `app.app`, который можно скачать прямо из репозитория.
